import { LinearGradient } from "expo-linear-gradient";
import styled from "styled-components/native";

export const LinearGradientPassword = styled(LinearGradient)`
    flex: 1;
    width: 100%;
    align-items: center;
    justify-content: center;
`

export const ContainerForgotPassword = styled.View`
    width: 260px;
    align-items: flex-start;
`

export const TextPassword = styled.Text`
    font-size: 14px;
    font-weight: bold;
    margin-bottom: 10px;
`